# AOF v9.4.1 Release Notes

## What You Can Do Now

You can install the current v9.4 release line from a CI-corrected tag.

This patch keeps the v9.4 capability unchanged: AOF can present a provider-backed action as a controlled execution candidate for operator go/no-go without claiming production execution authorization.

## Capability Delta

NEW:

- None.

UPDATED:

- `PCAP-PROVIDER-EXECUTION`: Patch release metadata and install references now point to the CI-corrected v9.4.1 tag.

REMOVED:

- None.

## Capability Matrix

See [docs/aof-capability-matrix.md](aof-capability-matrix.md).

In thirty seconds:

> v9.4.1 is the corrected v9.4 release tag. It preserves the controlled provider execution candidate capability and fixes release metadata consistency.

## Value Evidence

Before:

- The v9.4.0 tag existed, but GitHub Actions failed and the package lock still carried an older package version.

After:

- The install path, package metadata, active release manifest, and release notes point to a corrected patch tag.

Reduced work:

- New users do not have to diagnose whether the latest v9.4 install path points at a failed tag.

## Release Gate

`release-state-audit` still includes `provider-controlled-execution-candidate-audit` for `9.4.0` and later.

## Not Proven

This patch does not add new provider execution authority. It does not prove production provider execution safety, provider semantic correctness, credential safety, billing safety, actual rollback execution, or market adoption.
