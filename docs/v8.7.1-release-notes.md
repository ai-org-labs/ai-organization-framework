# AOF v8.7.1 Release Notes

`v8.7.1` is a patch release for the `v8.7` approval authenticity line.

## What Changed Since v8.7.0

- Tracks the canonical `TASK-107` done artifact required for fresh-clone CI
  roadmap reconstruction.
- Keeps the `v8.7` approval authenticity model intact: concrete provider
  target, independent human approval, adapter capability alignment, structured
  credential/budget/rollback boundaries, and preflight-only execution status.
- Keeps `v8.6` overclaim correction visible as blocked/read-only history.

## Verification Boundary

`v8.7.1` proves release reconstruction and governance checks from a fresh clone.
It does not prove live provider execution, rollback execution, credential
safety, billing safety, or provider output correctness.

