# AOF v6.6 Release Checklist

## Gate 1: Runtime Direction

- [x] `TASK-071` records the v6.6 Archmap-aware Mission Control work.
- [x] `situation-assess` reported `TASK-071` as `v6.6` before release closure.
- [x] `situation-assess` reported no truth conflicts.
- [x] `operating-goal` and `next-value-slice` point at Archmap-aware Mission Control.

## Gate 2: Archmap Contract

- [x] `docs/v6.6-archmap-integration.md` defines the architecture impact contract.
- [x] `docs/archmaps/aof-runtime-current.archmap` exists.
- [x] `.aof/artifacts/archmap/impact/TASK-071.json` records `archmap_update_required`.
- [x] The contract distinguishes `archmap_update_required`, `archmap_unaffected`, and `archmap_deferred_with_reason`.
- [x] The contract states that icons and diagrams are not quality proof by themselves.

## Gate 3: Council Disposition

- [x] Architecture Council review packet exists for `TASK-071`.
- [x] Archmap impact Council status is no longer `pending`.
- [x] `TASK-071` is closed only after Council approval evidence exists.

## Gate 4: Mission Control Boundary

- [x] Mission Control can project the current Archmap source ref.
- [x] Mission Control can project latest map-impact state.
- [x] Mission Control remains a read-only projection, not source of truth.
- [x] Release notes avoid claiming full visual comprehension or renderer integration.

## Gate 5: Runtime Support

- [x] `package.json` allows Node.js `>=22 <25`.
- [x] CI validates Node.js 22.
- [x] CI validates Node.js 24.
- [x] CLI warning treats Node.js 25+ as outside the supported runtime lane.
- [x] quickstart documents Node.js 22/24 support and Node.js 25+ non-support.

## Gate 6: Release State

- [x] package version is `6.6.0`.
- [x] bootstrap `aof_version` is `6.6.0`.
- [x] active release manifest points at v6.6 docs.
- [x] organization governance release contract points at `docs/v6.6-release-definition.md`.
- [x] README points at v6.6 release docs.
- [x] Quickstart points at `v6.6.0`.

## Gate 7: Verification

- [x] `node --test test/runtime-situation.test.js`
- [x] `node ./src/cli.js command-routing-audit --project .`
- [x] `node ./src/cli.js organization-verify --project .`
- [x] `node ./src/cli.js release-state-audit --project .`
- [x] `node ./src/cli.js decision-verify --project .`
- [x] `npm test`
- [x] `npm run smoke`

## Verification Evidence

- `situation-assess`: no truth conflicts, `TASK-071` is `v6.6` before closure.
- `command-routing-audit`: pass.
- `organization-verify`: pass.
- `release-state-audit`: pass for `6.6.0`.
- `decision-verify`: pass.
- `runtime-situation.test.js`: pass.
- `npm test`: pass.
- `npm run smoke`: pass.

## Sign-Off Rule

Do not sign off because an architecture diagram exists.

Sign off only if AOF now makes it harder for an operator or AI to confuse:

- architecture documentation with governed architecture impact
- Mission Control projection with source of truth
- visual icons with evidence
- pending Council review with approval
- Node version permissiveness with CI-validated support
