# AOF v10.2 Release Checklist

## Gate 1: Release Surface

- [x] `package.json` version is `10.2.0`.
- [x] Active release manifest points to v10.2 docs.
- [x] README current release references point at v10.2 docs.
- [x] Quickstart install path points at `v10.2.0`.

## Gate 2: Capability-First Value

- [x] `PCAP-EXTERNAL-PROVIDER-PILOT-SELECTION` is present in `.aof/product-capabilities.json`.
- [x] `docs/aof-capability-matrix.md` includes v10.2 and explains the delta in 30 seconds.
- [x] Release notes start with what users can do now.
- [x] `CRD-TASK-122-V102` records the capability delta.

## Gate 3: Runtime Evidence Chain

- [x] TASK-122 is recorded as v10.2 release work.
- [x] TASK-122 has work readiness, context integrity, Council review, role result, team output, requirement coverage, session export, parallel lane, and Archmap impact evidence.
- [x] `organization-verify` passes locally: `ok=true`, `total=614`, `errors=[]`.
- [x] `archmap-impact-audit` passes locally: `ok=true`, `scoped_task_count=52`, `errors=[]`.
- [x] `release-state-audit` passes locally: `ok=true`, `errors=[]`.

## Gate 4: Verification And Release

- [x] `node --test test/runtime-situation.test.js test/runtime-core-2.test.js` passes locally: `144 pass`, `0 fail`.
- [x] `npm test` passes locally.
- [x] `npm run smoke` passes locally.
- [x] clean checkout `release-state-audit` passes: `ok=true`, `errors=[]`.
- [ ] GitHub Actions pass on `main`.
- [ ] GitHub Actions pass on tag `v10.2.0`.
- [ ] GitHub Release `v10.2.0` is published.

## Sign-Off Boundary

v10.2 is release-ready only as an external provider pilot selection contract. It is not completed provider integration, provider write permission, production execution, market proof, or third-party validation completion.
