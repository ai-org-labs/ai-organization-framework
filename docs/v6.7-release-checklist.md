# AOF v6.7 Release Checklist

## Gate 1: Governance Audit Commands

- [x] `archmap-impact-audit` exists.
- [x] `review-provenance-audit` exists.
- [x] `evidence-independence-audit` exists.
- [x] Each audit has a schema, CLI registration, docs, and runtime tests.
- [x] Each audit has Council review evidence for the implementation task that introduced it.

## Gate 2: Release Sign-Off Integration

- [x] `release-state-audit` runs the three v6.7 governance audits.
- [x] `release-state-audit` reports governance audit summaries.
- [x] `release-state-audit` fails when a governance audit fails.
- [x] The release-state audit schema includes `governance_audits`.
- [x] `TASK-075` records Council approval for integrated release governance gates.

## Gate 3: Command Help Completeness

- [x] command registry entries have non-empty inputs and outputs.
- [x] `cli-help-benchmark` fails missing inputs or outputs.
- [x] `council-review-packet --help` reports inputs and outputs.
- [x] `cli-help-benchmark` passes after registry refresh.

## Gate 4: Archmap / Visibility Correctness

- [x] `docs/archmaps/aof-runtime-current.archmap` includes governance audit and release sign-off nodes.
- [x] `.aof/artifacts/archmap/impact/TASK-076.json` records release closure impact.
- [x] visibility latest Archmap work item uses task ordering when work item ids are present.
- [x] `test/runtime-situation.test.js` verifies Mission Control reports `TASK-076` as the latest Archmap work item.

## Gate 5: Release State

- [x] package version is `6.7.0`.
- [x] bootstrap `aof_version` is `6.7.0`.
- [x] active release manifest points at v6.7 docs.
- [x] organization governance release contract points at `docs/v6.7-release-definition.md`.
- [x] README points at v6.7 release docs.
- [x] Quickstart points at `v6.7.0`.

## Gate 6: Verification

- [x] `node --test test/runtime-core-2.test.js`
- [x] `node --test test/runtime-situation.test.js`
- [x] `node ./src/cli.js command-routing-audit --project .`
- [x] `node ./src/cli.js cli-help-benchmark --project .`
- [x] `node ./src/cli.js organization-verify --project .`
- [x] `node ./src/cli.js release-state-audit --project .`
- [x] `node ./src/cli.js archmap-impact-audit --project . --cutoff-task-id TASK-071`
- [x] `node ./src/cli.js review-provenance-audit --project . --cutoff-task-id TASK-071`
- [x] `node ./src/cli.js evidence-independence-audit --project . --cutoff-task-id TASK-071`
- [x] `node ./src/cli.js decision-verify --project .`
- [x] `npm test`
- [x] `npm run smoke`

## Verification Evidence

- `release-state-audit`: pass for `6.7.0`; includes Archmap impact, review provenance, and evidence independence gates.
- `archmap-impact-audit`: pass for `TASK-071` onward.
- `review-provenance-audit`: pass for done work at `TASK-071` onward.
- `evidence-independence-audit`: pass for done work at `TASK-071` onward.
- `cli-help-benchmark`: pass; no command help input/output gaps.
- `npm test`: pass.
- `npm run smoke`: pass.

## Sign-Off Rule

Do not sign off because tests pass, many artifacts exist, or a dashboard shows progress.

Sign off only if:

- the release surface aligns to `6.7.0`
- all v6.7 governance audits pass
- `release-state-audit` includes those audits as gates
- command help is complete enough for AI routing
- semantic uncertainty remains bounded rather than hidden
