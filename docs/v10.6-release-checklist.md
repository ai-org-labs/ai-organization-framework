# AOF v10.6 Release Checklist

## Gate 1: Release Surface

- [x] `package.json` version is `10.6.0`.
- [x] Active release manifest points to v10.6 docs.
- [x] README current release references point at v10.6 docs.
- [x] Quickstart install path points at `v10.6.0`.

## Gate 2: Capability-First Value

- [x] `PCAP-THIRD-PARTY-VALIDATION-RESULT-INGESTION` is present in `.aof/product-capabilities.json`.
- [x] `docs/aof-capability-matrix.md` includes v10.6 and explains the delta in 30 seconds.
- [x] Release notes start with what users can do now.
- [x] `CRD-TASK-126-V106` records the capability delta.

## Gate 3: Runtime Evidence Chain

- [x] TASK-126 is recorded as v10.6 release work.
- [x] TASK-126 has work readiness, context integrity, Council review, role result, team output, requirement coverage, session export, parallel lane, and Archmap impact evidence.
- [x] `organization-verify` passes (`ok=true`, `total=662`, `errors=[]`, 2026-08-05).
- [x] `archmap-impact-audit` passes (`ok=true`, `errors=[]`, 2026-08-05).
- [x] `release-state-audit` passes (`ok=true`, `errors=[]`, 2026-08-05).

## Gate 4: Verification And Release

- [x] `node --test test/runtime-situation.test.js test/runtime-core-2.test.js` passes (`144 pass, 0 fail`, 2026-08-05).
- [x] `npm test` passes (`35 pass, 0 fail` + runtime situation suite `10 pass, 0 fail`, 2026-08-05).
- [x] `npm run smoke` passes (`EXIT:0`, approved mock-provider outcome, 2026-08-05).
- [ ] clean checkout `release-state-audit` passes.
- [ ] GitHub Actions pass on `main`.
- [ ] GitHub Actions pass on tag `v10.6.0`.
- [ ] GitHub Release `v10.6.0` is published.

## Sign-Off Boundary

v10.6 is release-ready only as a third-party validation result ingestion contract. It is not actual external validation, third-party validation completion, external adoption, market proof, live provider execution, or production safety.
