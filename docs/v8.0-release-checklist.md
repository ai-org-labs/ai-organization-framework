# AOF v8.0 Release Checklist

- [x] `TASK-099` completed the runtime-backed v8.0 direction review.
- [x] `TASK-100` exists as the governed v8.0 implementation work item.
- [x] External runtime resource and use record schemas are implemented.
- [x] `external-runtime-resource-record` and `external-resource-use-record` are implemented.
- [x] `external-resource-audit` is implemented and passes.
- [x] `release-state-audit` includes `external-resource-audit` for v8.0+.
- [x] Mission Control projects external resource status from canonical artifacts.
- [x] Self-hosting evidence includes one bounded read-only external resource and use record.
- [x] TASK-100 has Council review, work readiness, context integrity, execution packet, multi-actor, parallel-lane, requirement coverage, session export, and Archmap impact evidence.
- [x] Command registry and CLI help include the new commands.
- [x] `release-state-audit` passes after active release is refreshed to v8.0.0.
- [x] `organization-verify` passes.
- [x] `command-routing-audit` passes.
- [x] `cli-help-benchmark` passes.
- [x] focused runtime tests pass.
- [x] `npm test` passes.
- [x] `npm run smoke` passes.

## Verification Evidence

- `node ./src/cli.js release-state-audit --project . --write-artifact .aof/context/active/release-state-audit.json`: pass, including `external-resource-audit`.
- `node ./src/cli.js organization-verify --project .`: pass, `374 / 374` checks.
- `node ./src/cli.js command-routing-audit --project .`: pass.
- `node ./src/cli.js cli-help-benchmark --project .`: pass, `124` help commands for `124` catalog commands.
- `node --test test/runtime-situation.test.js`: pass, `10 / 10`.
- `node --test test/runtime-core-2.test.js`: pass, `107 / 107`.
- `npm test`: pass.
- `npm run smoke`: pass.

## Not Proven

- Autonomous external provider execution is not implemented.
- Credential, billing, deploy, and production-write safety are not implemented.
- External semantic truth is not proven.
- Operator acceptance of real external workflows is not proven.
