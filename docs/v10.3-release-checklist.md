# AOF v10.3 Release Checklist

## Gate 1: Release Surface

- [x] `package.json` version is `10.3.0`.
- [x] Active release manifest points to v10.3 docs.
- [x] README current release references point at v10.3 docs.
- [x] Quickstart install path points at `v10.3.0`.

## Gate 2: Capability-First Value

- [x] `PCAP-PRODUCTION-EXECUTION-AUTHORIZATION-MODEL` is present in `.aof/product-capabilities.json`.
- [x] `docs/aof-capability-matrix.md` includes v10.3 and explains the delta in 30 seconds.
- [x] Release notes start with what users can do now.
- [x] `CRD-TASK-123-V103` records the capability delta.

## Gate 3: Runtime Evidence Chain

- [x] TASK-123 is recorded as v10.3 release work.
- [x] TASK-123 has work readiness, context integrity, Council review, role result, team output, requirement coverage, session export, parallel lane, and Archmap impact evidence.
- [x] `organization-verify` passes locally: `ok=true`, `total=626`, `errors=[]`.
- [x] `archmap-impact-audit` passes locally: `ok=true`, `errors=[]`.
- [x] `release-state-audit` passes locally: `ok=true`, `errors=[]`.

## Gate 4: Verification And Release

- [x] `node --test test/runtime-situation.test.js test/runtime-core-2.test.js` passes locally: `144 pass`, `0 fail`.
- [x] `npm test` passes locally.
- [x] `npm run smoke` passes locally.
- [x] clean checkout `release-state-audit` passes: `ok=true`, `errors=[]`.
- [ ] GitHub Actions pass on `main`.
- [ ] GitHub Actions pass on tag `v10.3.0`.
- [ ] GitHub Release `v10.3.0` is published.

## Sign-Off Boundary

v10.3 is release-ready only as a production execution authorization model. It is not completed provider integration, live provider execution, provider write safety, production credential safety, billing safety, incident recovery performance, market proof, or third-party validation completion.
