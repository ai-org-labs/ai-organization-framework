# AOF v9.4.2 Release Notes

## What You Can Do Now

You can install the current v9.4 release line from a CI-corrected tag whose release audit is reproducible from a clean checkout.

This patch keeps the v9.4 capability unchanged: AOF can present a provider-backed action as a controlled execution candidate for operator go/no-go without claiming production execution authorization.

## Capability Delta

NEW:

- None.

UPDATED:

- `PCAP-PROVIDER-EXECUTION`: Patch release evidence now includes the TASK-114 Council, role, and team artifacts required for release-state audit reproducibility in CI.

REMOVED:

- None.

## Capability Matrix

See [docs/aof-capability-matrix.md](aof-capability-matrix.md).

In thirty seconds:

> v9.4.2 is the clean-checkout corrected v9.4 release tag. It preserves the controlled provider execution candidate capability and fixes missing committed evidence required by release governance.

## Value Evidence

Before:

- Local release-state audit passed because ignored TASK-114 execution artifacts existed locally, but CI failed from a clean checkout.

After:

- The referenced TASK-114 role results, team output, and Council review artifacts are committed, so release-state audit can be reproduced from Git alone.

Reduced work:

- New users and CI no longer depend on invisible local artifacts to validate the v9.4 release line.

## Release Gate

`release-state-audit` still includes `provider-controlled-execution-candidate-audit` for `9.4.0` and later.

## Not Proven

This patch does not add new provider execution authority. It does not prove production provider execution safety, provider semantic correctness, credential safety, billing safety, actual rollback execution, or market adoption.
