# AOF v10.8 Release Checklist

## Gates

- [x] Role-to-actor routing resolves default Council seats before execution.
- [x] Deep-path planning includes Visionary, Builder, and Guardian.
- [x] Additional required specialist roles can be joined into a Council plan.
- [x] Missing required specialist roles fail closed before session persistence.
- [x] Council execution steps preserve `actor_ref`.
- [x] CLI accepts repeated `--required-role` for `council` / `council-exec`.
- [x] Session schema accepts routed execution evidence without breaking older sessions.

## Verification

- [x] `node --check src/runtime/council.js`
- [x] `node --check src/runtime/council-execution.js`
- [x] `node --check src/runtime/packet.js`
- [x] `node --check src/commands/council-exec.js`
- [x] `node --check src/commands/council.js`
- [x] `node --check src/cli-main.js`
- [x] `node --test test/runtime-core-3.test.js test/runtime-core-4.test.js`

## Release Boundary

v10.8.0 is release-ready only when full `npm test`, `npm run smoke`, `organization-verify`, and `release-state-audit` are green after cleanup of unrelated incomplete TASK-128 evidence.
