# AOF v6.8 Release Checklist

## Gate 1: Executable Quality Ledger Surface

- [x] `schemas/aof-quality-ledger-event.schema.json` exists.
- [x] `schemas/aof-quality-ledger-audit.schema.json` exists.
- [x] `quality-ledger-record` exists.
- [x] `quality-ledger-audit` exists.
- [x] `quality-ledger-record --help --json` exposes AI-readable inputs and outputs.
- [x] `quality-ledger-audit --help --json` exposes AI-readable inputs and outputs.

## Gate 2: Ledger Audit Behavior

- [x] committed ledger events are schema-valid.
- [x] `quality-ledger-audit` passes the committed planning-grade event.
- [x] `quality-ledger-audit` fails missing QIF refs.
- [x] `quality-ledger-audit` fails unresolved evidence refs.
- [x] `quality-ledger-audit` fails unsupported semantic truth claims.
- [x] `quality-ledger-audit` fails low-confidence or contradicted outcomes without governance escalation.
- [x] `quality-ledger-audit` fails events without state transition intent.

## Gate 3: Governance And Evidence

- [x] `TASK-078` is closed with Council review evidence.
- [x] `TASK-078` has Archmap impact disposition.
- [x] `TASK-078` is covered by review provenance audit.
- [x] `TASK-078` is covered by evidence independence audit.
- [x] Ledger presence is documented as traceability evidence, not proof of quality.
- [x] `release-state-audit` includes `quality-ledger-audit` for `6.8.0` and later releases.

## Gate 4: Release State

- [x] package version is `6.8.0`.
- [x] package-lock version is `6.8.0`.
- [x] active release manifest points at v6.8 docs.
- [x] organization governance release contract points at `docs/v6.8-release-definition.md`.
- [x] README points at v6.8 release docs.
- [x] Quickstart points at `v6.8.0`.
- [x] next release plan describes v6.8 release closure.

## Gate 5: Verification

- [x] `node --test test/runtime-core-2.test.js`
- [x] `node --test test/runtime-situation.test.js`
- [x] `node ./src/cli.js quality-ledger-audit --project .`
- [x] `node ./src/cli.js command-routing-audit --project .`
- [x] `node ./src/cli.js cli-help-benchmark --project .`
- [x] `node ./src/cli.js organization-verify --project .`
- [x] `node ./src/cli.js release-state-audit --project .`
- [x] `node ./src/cli.js archmap-impact-audit --project . --cutoff-task-id TASK-071`
- [x] `node ./src/cli.js review-provenance-audit --project . --cutoff-task-id TASK-071`
- [x] `node ./src/cli.js evidence-independence-audit --project . --cutoff-task-id TASK-071`
- [x] `node ./src/cli.js decision-verify --project .`
- [x] `npm test`
- [x] `npm run smoke`

## Verification Evidence

- `quality-ledger-audit`: pass; 1 committed ledger event, 0 failing checks, 0 semantic truth claims.
- `release-state-audit`: pass for `6.8.0`; includes Archmap impact, review provenance, evidence independence, and quality ledger gates.
- `review-provenance-audit`: pass for done work at `TASK-071` onward, including `TASK-079`.
- `evidence-independence-audit`: pass for done work at `TASK-071` onward, including `TASK-079`.
- `archmap-impact-audit`: pass for work at `TASK-071` onward, including `TASK-079`.
- `cli-help-benchmark`: pass; 101 help commands for 101 catalog commands.
- `organization-verify`: pass.
- `decision-verify`: pass.
- `npm test`: pass.
- `npm run smoke`: pass.

## Sign-Off Rule

Do not sign off because tests pass, many artifacts exist, a ledger event exists, or a release is tagged.

Sign off only if:

- the release surface aligns to `6.8.0`
- quality ledger commands and schemas are present
- negative ledger audit paths are covered
- governance audits still pass for implementation-grade work
- semantic uncertainty is escalated rather than hidden
- the ledger remains evidence, not truth
