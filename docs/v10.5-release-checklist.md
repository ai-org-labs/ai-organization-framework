# AOF v10.5 Release Checklist

## Gate 1: Release Surface

- [x] `package.json` version is `10.5.0`.
- [x] Active release manifest points to v10.5 docs.
- [x] README current release references point at v10.5 docs.
- [x] Quickstart install path points at `v10.5.0`.

## Gate 2: Capability-First Value

- [x] `PCAP-PROVIDER-BACKED-WORK-LIFECYCLE-SURFACE` is present in `.aof/product-capabilities.json`.
- [x] `docs/aof-capability-matrix.md` includes v10.5 and explains the delta in 30 seconds.
- [x] Release notes start with what users can do now.
- [x] `CRD-TASK-125-V105` records the capability delta.

## Gate 3: Runtime Evidence Chain

- [x] TASK-125 is recorded as v10.5 release work.
- [x] TASK-125 has work readiness, context integrity, Council review, role result, team output, requirement coverage, session export, parallel lane, and Archmap impact evidence.
- [x] `organization-verify` passes locally: `ok=true`, `total=650`, `errors=[]`.
- [x] `archmap-impact-audit` passes locally: `ok=true`, `errors=[]`.
- [x] `release-state-audit` passes locally: `ok=true`, `errors=[]`.

## Gate 4: Verification And Release

- [x] `node --test test/runtime-situation.test.js test/runtime-core-2.test.js` passes locally: `144 pass`, `0 fail`.
- [x] `npm test` passes locally.
- [x] `npm run smoke` passes locally.
- [x] clean checkout `release-state-audit` passes: `ok=true`, `errors=[]`.
- [ ] GitHub Actions pass on `main`.
- [ ] GitHub Actions pass on tag `v10.5.0`.
- [ ] GitHub Release `v10.5.0` is published.

## Sign-Off Boundary

v10.5 is release-ready only as a provider-backed work lifecycle product surface. It is not live provider execution, provider write safety, production safety, completed provider integration, external adoption, market proof, or third-party validation completion.
